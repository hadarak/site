<br><br><br><br><br><br><br><br><br>
<p id="notice"><%= notice %></p>
<div class="col-lg-12 text-center">
<p>
  <strong></strong>
  <%= image_tag(@idea.picture_url, :width => 400) if @idea.picture.present? %>
  <center><strong><font size="3", face="URW Gothic L,DEMI"><%= @idea.name %></font></strong></center>
</p>
</div>
<p>
  <strong><font size="3", face="URW Gothic L,DEMI">Descrição</font></strong>
  <pre><font size="2", face="URW Gothic L,DEMI"><%= @idea.description %></font></pre>
</p>
<br>
</br>
<br>
</br>
<br>
</br>
<br>
</br>
  <strong><font size="4", face="URW Gothic L,DEMI">Comentários</font></strong><br>
 <% @idea.comments.each do |comment| %>
  <div>
    <br><br><br><strong><%= comment.user_name %></strong>
    <br />
    <p><%= comment.body %><p>
  </div>
<% end %>
<%= render 'comments/form' %>
<% if (user_signed_in? && current_user.username == "admin") %>           
  <%= link_to 'Editar', edit_idea_path(@idea)  %> |
  <%= link_to 'Voltar', ideas_path %>   
           
<% else %>
  <%= link_to 'Voltar', ideas_path %>
<% end %>
